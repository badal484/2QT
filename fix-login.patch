--- web/app/login/page.tsx
+++ web/app/login/page.tsx
@@ -4,6 +4,6 @@
 import Link from "next/link";
-import { useRouter } from "next/navigation";
+import { useRouter, useSearchParams } from "next/navigation";
 import { ArrowRight, ArrowLeft } from "lucide-react";
 import { api } from "../lib/api";
 import { useAuth } from "../lib/auth";
 
@@ -11,6 +11,7 @@
 export default function Login() {
   const router = useRouter();
+  const searchParams = useSearchParams();
   const { login } = useAuth();
 
   const [phone, setPhone] = useState("");
@@ -39,12 +40,16 @@
     try {
       const data = await api.verifyOtp(phone, otp, name.trim() || undefined);
       login(data.user);
-      if (data.user.role === "super_admin") router.push("/admin");
-      else if (data.user.role === "chef") router.push("/kitchen");
-      else if (data.user.role === "rider") router.push("/rider");
-      else router.push("/menu");
+      
+      const redirectUrl = searchParams.get("redirect");
+      if (redirectUrl) {
+        router.push(redirectUrl);
+      } else if (data.user.role === "super_admin") router.push("/admin");
+      else if (data.user.role === "chef") router.push("/kitchen");
+      else if (data.user.role === "rider") router.push("/rider");
+      else router.push("/menu");
     } catch (err: unknown) {
